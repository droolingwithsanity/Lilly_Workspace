const fs = require('fs');
const path = require('path');

const PROJECTS = [
  'lillymerg','keke_fileshare','qwen_translate','familywall','project_kiki',
  'media_gallery','ecommerce_store','trading_bot','taxhacker','file_manager',
  'wordpress_store','gitea','jellyfin','dify','buildy_seo','dws_mern',
  'lillyos','lillyshader','spawn','local_llm_gui','genesis','ader',
];

const outDir = path.join(process.cwd(), 'public', 'screenshots');
fs.mkdirSync(outDir, { recursive: true });

for (const name of PROJECTS) {
  const fp = path.join(outDir, name + '.png');
  if (!fs.existsSync(fp)) {
    // Create a small valid 1x1 white PNG as placeholder
    const png = Buffer.from([
      0x89,0x50,0x4E,0x47,0x0D,0x0A,0x1A,0x0A,
      0x00,0x00,0x00,0x0D,0x49,0x48,0x44,0x52,
      0x00,0x00,0x00,0x01,0x00,0x00,0x00,0x01,
      0x08,0x06,0x00,0x00,0x00,0x1F,0x15,0xC4,
      0x89,0x00,0x00,0x00,0x0A,0x49,0x44,0x41,
      0x54,0x78,0x9C,0x62,0x00,0x00,0x00,0x00,
      0x00,0xFF,0xFF,0x00,0x03,0x00,0x00,0x00,
      0x01,0xAE,0xDE,0xFE,0xE3,0x00,0x00,0x00,
      0x00,0x49,0x45,0x4E,0x44,0xAE,0x42,0x60,
      0x82,
    ]);
    fs.writeFileSync(fp, png);
  }
}

console.log(`✓ Placeholder screenshots ready in ${outDir}`);
