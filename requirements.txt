fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
httpx>=0.25.0
websockets>=12.0
aiohttp>=3.9.0
python-multipart>=0.0.6
redis>=4.5.0

# ─── Persona fine-tuning (LoRA) ───
torch>=2.0.0
transformers>=4.40.0
datasets>=2.15.0
peft>=0.8.0
accelerate>=0.25.0
sentencepiece>=0.1.99
tokenizers>=0.15.0
# Unsloth — fast LoRA/QLoRA on GPU (~2x faster, ~70% less VRAM).
# Requires CUDA. Falls back to classic PEFT training when no GPU is present.
unsloth>=2025.1.0

# ─── Auth0 authentication ───
auth0-server-python>=1.0.0b13
python-dotenv>=1.0.0

# ─── Whisper STT (runs inside container on :8001) ───
faster-whisper-server==0.0.1

# ─── Vision / Object Detection (YOLOv8) ───
ultralytics>=8.1.0
opencv-python-headless>=4.8.0
insightface>=0.7.0

# ─── BLE advertisement scan (container-direct, via mounted BlueZ D-Bus) ───
bleak>=0.22.0
