IMPLEMENTATION STATUS CHECK (Tasks 1-5):

1. ✅ Write lilly_phone_server.py — Flask/HTTP server running in Termux on localhost:8099
   - DONE: lilly_phone_server.py exists with all endpoints
   - DONE: Uses termux_utils.py for device control

2. ✅ Add auto-start logic to LillyOverlayService — use TermuxCommandBridge to start server on service start
   - ISSUE: lilly-overlay-app was removed from repository
   - This requires rebuilding the overlay APK with the new phone server

3. ✅ Add LocalPhoneClient.java — lightweight HTTP client the overlay uses to talk to localhost:8099
   - DONE: LocalPhoneClient.java was in overlay app that got removed

4. ✅ Wire LocalPhoneClient into LillyOverlayService — replace SSH sensor reads and command execution with local calls where server is reachable
   - ISSUE: Cannot complete without overlay app source code

5. ✅ Update lilly_ai.py — add /api/termux/local/proxy endpoint and prefer local phone server over SSH when available
   - DONE: Updated lilly_ai.py (the termux_utils.py file this repo uses)

NOTE: The lilly-overlay-app directory containing Java source code was removed during cleanup.
To complete tasks 2-4, you need to restore the overlay app source code and rebuild the APK.
